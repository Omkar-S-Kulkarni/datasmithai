In this basically I am taking input as an image audio and basic text so basically we are autonomously running this agent where
I have used mainly 2 late approach
one is : API 
second one is :  ollama based models 
so we are running everything locally using olama and also we are using openrouter 
if the credentials got over we can just do this
ollama install 
ollama --verison 
ollama ls 
ollama serve


and to run the openrouter based project 
$env:OPENROUTER_API_KEY="add in this only "  and dont give " " around that  in terminal or we can give in .env file which is better option.
